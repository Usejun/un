use io
use flow

# IO w 모드: 파일 없어도 생성
s = io.open("test_poolio_tmp.txt", "w")
io.write(s != none)

using pool: pool = flow.spawn(2)
f = fn(x) -> x * 2
res = pool.map(f, (1, 2, 3))
io.write(res)
pool.close()
io.write("done")
